# bash_profile

# set alsa options - quiet mode
#amixer -q set Speaker mute
#amixer -q set PCM mute
#amixer -q set Headphone 100%
#amixer -q set Headphone unmute
#amixer -q set Master 100%
#amixer -q set Master unmute

if [ -f ~/.bashrc ]; then
   source ~/.bashrc
fi
